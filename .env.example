# LinkedIn API Credentials (for OAuth)
# Get these from https://www.linkedin.com/developers/apps

LINKEDIN_CLIENT_ID=your_client_id_here
LINKEDIN_CLIENT_SECRET=your_client_secret_here

# Optional: OAuth scopes. Default: r_ads,r_ads_reporting,rw_ads
# To get ONE account per LinkedIn user (same user on localhost and Railway), add the "Sign In with LinkedIn using OpenID Connect" product to your app, then set:
# LINKEDIN_SCOPES=openid,profile,r_ads,r_ads_reporting,rw_ads

# Server port
PORT=3000

# For deployment: your public app URL (no trailing slash). Required for LinkedIn OAuth.
# Example: https://your-app-name.railway.app
# BASE_URL=https://your-app.example.com

# Supabase + JWT (required for login). Users sign in with LinkedIn; selected ad account is stored per user.
# Get these from your Supabase project: Settings → API. Use the service_role key (server-side only).
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
# JWT_SECRET=your_random_secret_here
